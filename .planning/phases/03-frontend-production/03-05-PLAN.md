---
phase: 03-frontend-production
plan: 05
type: execute
wave: 4
depends_on: [03-03B]
files_modified: [frontend/package.json]
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "npm run build:client completes successfully without module resolution errors"
    - "Frontend production build generates dist/ directory with bundled assets"
    - "TypeScript compilation passes with 0 errors from missing dependencies"
  artifacts:
    - path: "frontend/package.json"
      provides: "@radix-ui/react-alert-dialog dependency"
      contains: "@radix-ui/react-alert-dialog"
  key_links:
    - from: "frontend/client/pages/JobProgress.tsx"
      to: "@/components/ui/alert-dialog"
      via: "import statement"
      pattern: "import.*AlertDialog.*from.*alert-dialog"
---

<objective>
Close build failure gap by installing missing @radix-ui/react-alert-dialog dependency required by JobProgress cancellation dialog.

Purpose: Enable production deployment by fixing Vite build failure caused by missing peer dependency.
Output: Working production build with all shadcn/ui AlertDialog dependencies satisfied.
</objective>

<execution_context>
@/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-production/03-VERIFICATION.md
@.planning/phases/03-frontend-production/03-03B-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install missing @radix-ui/react-alert-dialog dependency</name>
  <files>frontend/package.json</files>
  <action>
Install @radix-ui/react-alert-dialog package to satisfy JobProgress AlertDialog component dependency.

**Why this is needed:**
- Plan 03-03B created JobProgress.tsx with AlertDialog for cancellation confirmation
- AlertDialog is a shadcn/ui component that imports from @radix-ui/react-alert-dialog
- Package was not installed when AlertDialog was added
- Build fails at Vite rollup resolution step with "Cannot resolve '@radix-ui/react-alert-dialog'"

**Context from 03-VERIFICATION.md:**
- Build failure blocks production deployment (Truth #1, #2, #14 failed)
- 81 TypeScript errors from missing radix packages (only alert-dialog actively used)
- Development server works (runtime resolution) but production build fails
- 16/18 truths verified, only build-related failures remain

**Implementation:**
```bash
cd frontend
npm install @radix-ui/react-alert-dialog
```

**Package details:**
- Package: @radix-ui/react-alert-dialog
- Purpose: Peer dependency for shadcn/ui AlertDialog component
- Used by: frontend/client/pages/JobProgress.tsx (cancellation confirmation dialog)
- Version: Use npm's default (latest stable, should be ^1.x.x to match other @radix-ui packages)

**What NOT to do:**
- Do NOT remove unused shadcn/ui components (out of scope for gap closure)
- Do NOT install all missing @radix-ui packages (only alert-dialog is actively used)
- Do NOT modify JobProgress.tsx or alert-dialog.tsx (code is correct, just needs dependency)
  </action>
  <verify>
    <automated>cd frontend && npm list @radix-ui/react-alert-dialog</automated>
  </verify>
  <done>@radix-ui/react-alert-dialog appears in frontend/package.json dependencies section</done>
</task>

<task type="auto">
  <name>Task 2: Verify production build succeeds</name>
  <files></files>
  <action>
Run TypeScript compilation and Vite production build to confirm gap closure.

**Verification steps:**

1. **TypeScript compilation check:**
```bash
cd frontend
npx tsc --noEmit
```
Expected: 0 errors (all noImplicitAny violations were fixed in 03-04A)

2. **Production build:**
```bash
cd frontend
npm run build:client
```
Expected: Build completes successfully, generates frontend/dist/ directory with bundled assets

3. **Dependency verification:**
```bash
cd frontend
npm list @radix-ui/react-alert-dialog
```
Expected: Shows version installed without "UNMET DEPENDENCY" warning

**What success looks like:**
- Vite build completes without "Cannot resolve" errors
- dist/ directory contains index.html, assets/ with JS/CSS bundles
- No TypeScript errors from missing @radix-ui imports
- Build output shows "✓ built in Xms" success message

**If build still fails:**
- Check for other missing @radix-ui packages (unlikely, only alert-dialog actively used)
- Verify package-lock.json updated correctly
- Try `rm -rf node_modules && npm install` to rebuild dependency tree
  </action>
  <verify>
    <automated>cd frontend && npm run build:client 2>&1 | grep -q "built in" && echo "Build succeeded"</automated>
  </verify>
  <done>Vite build completes successfully and frontend/dist/ directory exists with bundled assets</done>
</task>

</tasks>

<verification>
## Overall Phase Checks

After completing this plan, Phase 03 verification truths should update:

| Truth | Before | After |
|-------|--------|-------|
| Truth #1: Frontend builds without missing dependency errors | ✗ FAILED | ✓ VERIFIED |
| Truth #2: npm run build:client completes successfully | ✗ FAILED | ✓ VERIFIED |
| Truth #14: Application builds successfully with all dependencies installed | ✗ FAILED | ✓ VERIFIED |

**Manual verification (from VERIFICATION.md section):**

After this plan completes, run the TypeScript Compilation test from VERIFICATION.md section "4. TypeScript Compilation (Post-Dependency Fix)":

```bash
cd frontend
npx tsc --noEmit
npm run build:client
```

Expected outcome:
- TypeScript compilation succeeds with 0 errors
- Vite build completes successfully
- No `as any` type assertions in Phase 03 code (already verified in 03-04A)

**Phase 03 completion:**
- Gap closure brings verification score from 16/18 to 18/18 truths verified
- All 22 requirements (FE-01 through FE-22) remain satisfied
- Production build now works, enabling deployment
</verification>

<success_criteria>
**Measurable completion:**
1. `npm list @radix-ui/react-alert-dialog` shows package installed (exit code 0)
2. `npm run build:client` completes successfully (exit code 0)
3. `frontend/dist/index.html` exists after build
4. `grep "@radix-ui/react-alert-dialog" frontend/package.json` finds dependency entry
5. No "Cannot resolve" errors in build output

**Phase-level success:**
- VERIFICATION.md gaps section shows 0 gaps (was 1)
- Phase 03 score increases from 17/18 to 18/18 must-haves verified
- Production deployment unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-production/03-05-SUMMARY.md` following the standard summary template.

Include:
- Dependency installed: @radix-ui/react-alert-dialog version
- Build verification results (npx tsc output, npm run build:client output)
- Final verification score: 18/18 truths verified
- Phase completion status: All 22 requirements satisfied, 6 original plans + 1 gap closure plan executed
</output>
