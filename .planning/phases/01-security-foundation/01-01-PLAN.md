---
phase: 01-security-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/jest.config.js
  - frontend/server/__tests__/setup.ts
  - frontend/server/config/env.ts
  - frontend/server/config/logger.ts
  - frontend/server/middleware/errorHandler.ts
  - frontend/package.json
  - tests/test_env_validation.py
  - worker.py
autonomous: true
requirements: [SEC-12, SEC-13, SEC-16]

must_haves:
  truths:
    - "Server fails fast at startup if required environment variables are missing"
    - "Worker fails fast at startup if required environment variables are missing"
    - "Production error responses never expose stack traces"
    - "All errors are logged to GCP Cloud Logging with full details"
    - "Hardcoded GCP identifiers are replaced with environment variables"
  artifacts:
    - path: "frontend/server/config/env.ts"
      provides: "Environment variable validation with envalid"
      exports: ["env"]
    - path: "frontend/server/config/logger.ts"
      provides: "Winston logger with Cloud Logging transport"
      exports: ["logger"]
    - path: "frontend/server/middleware/errorHandler.ts"
      provides: "Production error handler middleware"
      exports: ["errorHandler"]
    - path: "frontend/jest.config.js"
      provides: "Jest configuration for Node.js testing"
    - path: "tests/test_env_validation.py"
      provides: "Python worker env validation tests"
    - path: "worker.py"
      provides: "Worker with env validation at startup"
  key_links:
    - from: "frontend/server/index.ts"
      to: "frontend/server/config/env.ts"
      via: "import at startup"
      pattern: "import.*env.*from.*config/env"
    - from: "frontend/server/middleware/errorHandler.ts"
      to: "frontend/server/config/logger.ts"
      via: "logger import"
      pattern: "import.*logger.*from.*config/logger"
    - from: "worker.py"
      to: "validate_environment"
      via: "function call before subscription"
      pattern: "validate_environment\\(\\)"
---

<objective>
Establish foundational security infrastructure: environment validation, structured logging, and production error handling.

Purpose: All subsequent security middleware depends on validated environment variables and proper error handling. This plan creates the infrastructure that other plans wire into.

Output: Environment validation at startup for both Express server and Python worker, Winston logger with Cloud Logging, production error handler that hides stack traces.
</objective>

<execution_context>
@/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-foundation/01-RESEARCH.md

# Existing code to modify
@frontend/server/index.ts
@frontend/package.json
@worker.py
</context>

<tasks>

<task type="auto">
  <name>Task 0: Set up Jest test infrastructure for Express server</name>
  <files>frontend/jest.config.js, frontend/server/__tests__/setup.ts, frontend/package.json</files>
  <action>
Install test dependencies:
```bash
cd frontend && npm install --save-dev jest @types/jest supertest @types/supertest ts-jest
```

Create Jest configuration for Node.js environment in `frontend/jest.config.js`:
- preset: ts-jest
- testEnvironment: node
- roots: ['<rootDir>/server']
- setupFilesAfterEnv: ['<rootDir>/server/__tests__/setup.ts']

Create test setup file `frontend/server/__tests__/setup.ts`:
- Mock Firestore client (jest.mock('@google-cloud/firestore'))
- Mock Storage client (jest.mock('@google-cloud/storage'))
- Mock PubSub client (jest.mock('@google-cloud/pubsub'))
- Set test environment variables (NODE_ENV=test, etc.)

Add npm script to package.json: "test": "jest"
  </action>
  <verify>
    <automated>cd /Users/aaron/Desktop/VScode/AutoEncoder2025/frontend && npm test -- --passWithNoTests</automated>
  </verify>
  <done>Jest runs without errors in the frontend directory</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Create environment validation with envalid</name>
  <files>frontend/server/config/env.ts, frontend/server/__tests__/config/env.test.ts</files>
  <behavior>
    - Test: cleanEnv throws when GOOGLE_CLOUD_PROJECT missing
    - Test: cleanEnv throws when GCS_BUCKET_NAME missing
    - Test: cleanEnv throws when SESSION_SECRET missing
    - Test: cleanEnv passes when all required vars present
    - Test: PORT defaults to 5001 when not specified
    - Test: NODE_ENV defaults to development when not specified
  </behavior>
  <action>
Install envalid:
```bash
cd frontend && npm install envalid
```

Create `frontend/server/config/env.ts`:
- Import cleanEnv, str, port from envalid
- Validate required: GOOGLE_CLOUD_PROJECT, GCS_BUCKET_NAME, SESSION_SECRET, FRONTEND_URL
- Validate optional with defaults: PORT (5001), NODE_ENV (development)
- Export validated env object

Create test file verifying validation behavior.
  </action>
  <verify>
    <automated>cd /Users/aaron/Desktop/VScode/AutoEncoder2025/frontend && npm test -- server/__tests__/config/env.test.ts</automated>
  </verify>
  <done>Environment validation throws descriptive errors for missing required variables</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Winston logger with Cloud Logging transport</name>
  <files>frontend/server/config/logger.ts, frontend/server/__tests__/config/logger.test.ts</files>
  <behavior>
    - Test: logger.info() writes structured JSON in production
    - Test: logger.error() includes stack trace in log message
    - Test: logger uses Console transport in test/development
  </behavior>
  <action>
Install logging dependencies:
```bash
cd frontend && npm install winston @google-cloud/logging-winston
```

Create `frontend/server/config/logger.ts`:
- Import winston and LoggingWinston from @google-cloud/logging-winston
- Create LoggingWinston transport with projectId from env
- Add Console transport only for non-production (format.simple())
- Export logger instance

Create test file verifying logging behavior.
  </action>
  <verify>
    <automated>cd /Users/aaron/Desktop/VScode/AutoEncoder2025/frontend && npm test -- server/__tests__/config/logger.test.ts</automated>
  </verify>
  <done>Logger writes to console in development, Cloud Logging in production</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create production error handler middleware</name>
  <files>frontend/server/middleware/errorHandler.ts, frontend/server/__tests__/middleware/errorHandler.test.ts</files>
  <behavior>
    - Test: In production (NODE_ENV=production), error response excludes stack trace
    - Test: In production, error response uses generic message "Internal server error"
    - Test: In development, error response includes original error message
    - Test: Error handler logs full error details via logger
    - Test: Error handler returns 500 status code for unhandled errors
    - Test: Error handler preserves custom status codes (e.g., 400, 401, 403)
  </behavior>
  <action>
Create `frontend/server/middleware/errorHandler.ts`:
- Import logger from ./config/logger
- Import env from ./config/env
- Create Express error middleware (err, req, res, next) signature
- Log full error: logger.error('Request error', { message, stack, path, method, userId: req.user?.id })
- Return generic message in production: { error: 'Internal server error' }
- Return detailed message in development: { error: err.message, stack: err.stack }
- Preserve err.status if set, default to 500

Create test file using supertest to verify error responses.
  </action>
  <verify>
    <automated>cd /Users/aaron/Desktop/VScode/AutoEncoder2025/frontend && npm test -- server/__tests__/middleware/errorHandler.test.ts</automated>
  </verify>
  <done>Production errors return generic messages, full details logged server-side</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create Python worker environment validation tests</name>
  <files>tests/test_env_validation.py</files>
  <behavior>
    - Test: validate_environment() raises SystemExit if GOOGLE_CLOUD_PROJECT missing
    - Test: validate_environment() raises SystemExit if GCS_BUCKET_NAME missing
    - Test: validate_environment() raises SystemExit if PUBSUB_SUBSCRIPTION_ID missing
    - Test: validate_environment() returns True when all required vars present
  </behavior>
  <action>
Create `tests/test_env_validation.py`:
- Import validate_environment from worker module
- Use pytest fixtures to temporarily unset env vars via monkeypatch
- Use pytest.raises(SystemExit) to verify worker exits on missing vars
- Test passes when all required vars are present

Tests should import the actual validate_environment function from worker.py (created in Task 5).
  </action>
  <verify>
    <automated>cd /Users/aaron/Desktop/VScode/AutoEncoder2025 && python -m pytest tests/test_env_validation.py -x -v</automated>
  </verify>
  <done>Python tests verify worker env validation behavior</done>
</task>

<task type="auto">
  <name>Task 5: Implement worker.py environment validation</name>
  <files>worker.py</files>
  <action>
Update `worker.py` to validate environment variables at startup (fail fast if missing):

1. Add validate_environment() function near the top of the file (after imports):
```python
def validate_environment():
    """Validate required environment variables. Exits if any are missing."""
    required_vars = {
        'GOOGLE_CLOUD_PROJECT': PROJECT_ID,
        'GCS_BUCKET_NAME': BUCKET_NAME,
        'PUBSUB_SUBSCRIPTION_ID': SUBSCRIPTION_ID,
    }

    missing = [name for name, value in required_vars.items() if not value]

    if missing:
        logger.error(f"Missing required environment variables: {', '.join(missing)}")
        logger.error("Set these variables before starting the worker.")
        sys.exit(1)

    logger.info("Environment validation passed.")
    return True
```

2. Import sys at the top of the file (add to existing imports).

3. Call validate_environment() in the `if __name__ == "__main__":` block BEFORE creating the subscriber:
```python
if __name__ == "__main__":
    parser = argparse.ArgumentParser(...)
    args = parser.parse_args()
    _processing_mode = args.mode

    # Fail fast if required env vars are missing
    validate_environment()

    subscriber = pubsub_v1.SubscriberClient()
    # ... rest of startup code
```

This ensures the worker exits immediately with a clear error message if critical environment variables are missing, rather than failing later with cryptic errors.
  </action>
  <verify>
    <automated>cd /Users/aaron/Desktop/VScode/AutoEncoder2025 && python -m pytest tests/test_env_validation.py -x -v</automated>
  </verify>
  <done>Worker validates environment at startup and exits with clear error if variables missing</done>
</task>

</tasks>

<verification>
- Jest configuration allows running Express server tests
- Environment validation catches missing required variables (Express and Python worker)
- Logger sends to Cloud Logging in production mode
- Error handler never exposes stack traces when NODE_ENV=production
- Python worker env validation tests pass
- Worker.py validates env vars before subscribing to Pub/Sub
</verification>

<success_criteria>
- `npm test` passes in frontend directory
- Server fails fast at startup with missing env vars (clear error message)
- Worker fails fast at startup with missing env vars (clear error message)
- Error responses in production mode contain only { error: "Internal server error" }
- Hardcoded GCP identifiers in index.ts can be replaced with env imports
- `python -m pytest tests/test_env_validation.py` passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-foundation/01-01-SUMMARY.md`
</output>
